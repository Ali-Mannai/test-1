pipeline {
  agent any
    // triggers {
    //     // Déclencher le build lorsqu'un push est effectué sur le dépôt GitHub
    //     githubPush()
    // }
  
  stages {
    stage('build') {
      steps {
        echo 'build completed.'
      }
    }

  }
}
